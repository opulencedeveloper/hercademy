import Portal from "../UI/Portal";

const Splash = () => {
  return (
      <div className="fixed top-0 right-0 h-screen w-full bg-primary overflow-hidden scrollbar-hide">aaaa</div>
  );
};

export default Splash;
